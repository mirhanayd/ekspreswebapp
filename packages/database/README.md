# @ekspres/database

This package is a placeholder for the future database boundary.

## Boundary Rules

- This package must NOT be imported by any browser-side code (passenger-web, admin-web client components).
- It is intended to encapsulate Drizzle ORM and Postgres connection logic.
- ORM and migrations will be implemented in a later task.
